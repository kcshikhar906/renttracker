rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /receipts/{userId}/{allPaths=**} {
      // Allow read/write only if the authenticated user matches the path userId
      allow read, write: if request.auth != null && request.auth.uid == userId;
    }
  }
}
